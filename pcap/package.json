{
  "name": "pcap",
  "version": "0.1.0",
  "description": "Reads all packets to list services used on network",
  "main": "index.js",
  "scripts": {
    "start": "node ./built/index.js",
    "build": "tsc",
    "beautify": "prettier --config ../.prettier.json ./**/*.js --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vivi",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.13.4",
    "amqplib": "^0.5.5"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
